<?xml version="1.0" encoding="utf-8"?>
<com.singleevent.sdk.Custom_View.HeaderView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Title -->
    <TextView
        android:id="@+id/header_view_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="5dp"
        android:fontFamily="@string/roboto_regular"
        android:textColor="@android:color/white"
        android:textSize="14sp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/header_view_sub_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:singleLine="true"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="10dp"
        android:fontFamily="@string/roboto_regular"
        android:textColor="@android:color/white"
        android:textSize="12sp" />


</com.singleevent.sdk.Custom_View.HeaderView>